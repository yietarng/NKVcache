---
title: "Sub-Context KV Cache (NOC)"
metatags:
    description: "Position-independent KV reuse for repeated sub-contexts in agentic workloads."
---

Agentic sessions resubmit large fractions of their context every turn (tool
outputs, RAG fragments, system-prompt blocks) but RadixAttention's prefix
match only discovers reuse when a span recurs at the *same* offset from
position 0. This feature (internal name **NOC**) extends KV reuse to any
recurring sub-context, independent of position, by keying a chunk's cached
KV by content hash instead of tree path, and re-encoding it for its new
position instead of recomputing it.

## Status

The full mechanism described here is implemented end to end: matching,
reuse, and registration (the previous gap -- an index nothing ever
populated -- is closed; see
[Registration](#registration-populating-the-index)). What's verified
without a GPU, and what still needs GPU validation before production use,
is called out per section below -- see "What still needs GPU validation"
at the end for the summary.

## Algorithm

1. **Scan (CPU).** After the ordinary prefix-radix match, scan the
   unmatched suffix of the request's tokens against a content-addressed
   index of previously-computed sub-contexts. Two sources: explicit tags
   the caller attaches to the request (a tool-output span, a RAG fragment
   id, ...), and automatic detection via rolling-hash probing.
2. **Reposition.** A matched chunk's cached K was computed with RoPE baked
   in at its original position. RoPE's rotation composes additively
   (`Rot(p_new) = Rot(p_new - p_old) @ Rot(p_old)`), so the chunk's K is
   moved to its new position by rotating it once more by the offset delta
   -- no recomputation, no recovering the pre-rotation value. V carries no
   positional encoding and is copied as-is.
3. **Compute the rest from scratch.** Tokens not covered by any match
   (the "glue" between sub-contexts) go through a normal forward pass, as
   does a small selectively-recomputed subset of each match (see below).

## Why reuse alone is an approximation

Repositioning fixes a reused chunk's *position*, not its *content*: the
chunk's K/V were computed while attending to whatever preceded it the
first time it was seen, and now sit next to different neighbors. Following
CacheBlend/EPIC (`https://github.com/DerekHJH/epic`), a **Boundary
Recompute Zone (BRZ)** around each stitched boundary is forced through a
real forward pass instead of trusted as pure reuse, bounding the error:
for adjacent segments A and B, `BRZ(A, B) = Last_k(A) union First_k(B)`,
with the window `k` (`--subcontext-brz-window`, default 32, one of `{0,
16, 32, 64}`) fixed in tokens rather than scaled to chunk length. A
reused chunk's own leading `k` tokens are always recomputed -- whatever
precedes it in the new sequence is essentially never what preceded it
originally. Its trailing `k` tokens are *additionally* recomputed only
when another reused chunk is stitched immediately after it with no fresh
glue in between (a genuine A/B stitch); a chunk followed by freshly
computed glue needs no trailing repair, since glue is already computed
against this chunk's true tail. Setting `k` to 0 recovers pure
PromptCache-style reuse.

## MVP scope

Restricted to dense, standard-RoPE models (e.g. Llama-style) without MLA,
mrope, SWA, Mamba, or the CUDA-graph fast path -- the surface where the
delta-rotation trick and the "exclude reused tokens from the forward pass"
batch construction are both straightforward. Wiring the remaining backends
is follow-up work, not a design limitation of the approach itself.

## Architecture

### Content-addressed index (`mem_cache/subcontext/`)

- `subcontext_types.py` -- `SubContextTag` (explicit boundary), `SubContextEntry`
  (a registered chunk: content hash, token ids, original position, KV-pool
  slot indices), `SubContextMatch` (one resolved hit in a new request),
  `RecomputePlan` (which offsets of a match get force-recomputed).
- `subcontext_index.py` -- `SubContextIndex`: sha256-content-hash keyed
  store, backed by a fixed-size ring buffer of KV-pool slots reserved once
  at startup (see [Registration](#registration-populating-the-index)
  below). `register()` reserves ring space for a new entry, skipping past
  any *referenced* (in-flight) occupant and evicting only unreferenced
  ones in its way (`inc_ref`/`dec_ref` mirror
  `RadixCache.inc_lock_ref`/`dec_lock_ref`) -- wraps to the start of the
  buffer when the tail is too small, and gives up (returns `None`, no
  side effects) if a full pass finds no room. A probe-hash bucket
  (`PROBE_LEN`-token prefix hash) narrows scan candidates before a full
  verify.
- `subcontext_scanner.py` -- `scan(index, token_ids, explicit_tags, start, end)`:
  greedy, longest-match-first scan restricted to `token_ids[start:end]`
  (`start` excludes the region already covered by the ordinary prefix
  match, which is strictly cheaper reuse and always wins where it
  applies; `end` leaves at least the request's own last token
  uncovered -- see the attention-backend section below).
- `deviation_recompute.py` -- `plan_boundary_recompute_zones`: applies BRZ
  across every match in a request at once (needs the full, query-ordered
  match list to detect which pairs are actually stitched together), turning
  each into a `RecomputePlan`.

**Tested**: `test/registered/unit/mem_cache/test_subcontext_cache.py` --
27 cases covering the scanner's longest-match, explicit-tag-priority, and
last-token-cap invariants; the index's ring-buffer reservation, wraparound,
dedup, oversized-span rejection, and referenced-entry-survives-eviction
invariants; and `RecomputePlan` boundary math. Pure CPU/tensor logic, runs
on any runner.

### RoPE delta-repositioning (`layers/rotary_embedding/reposition.py`)

`reposition_key(cached_key, delta_positions, inv_freq, is_neox_style)`
recomputes cos/sin directly from `delta_positions * inv_freq` (rather than
indexing the model's position-capped cache, since a delta can be negative)
and reapplies `apply_rotary_emb` on top of the already-rotated K. Callers
pass only the rotary slice for partial-rotary models.

**Tested**: rotation composition (`Rot(delta) @ Rot(p_old) == Rot(p_old +
delta)`, both NeoX and GPT-J styles) verified numerically against the
repo's real `apply_rotary_emb`, plus the zero-delta identity case.

### Explicit tagging API

`GenerateReqInput.subcontext_tags` (a list of `{"subcontext_id", "start",
"end"}`, token offsets into the request's own tokens) flows through
`TokenizedGenerateReqInput.subcontext_tags` (`managers/io_struct.py`,
`managers/tokenizer_manager.py`) into `Req.subcontext_tags`
(`managers/schedule_batch.py`). Ignored entirely unless
`--enable-subcontext-kv-cache` is set.

### Registration: populating the index

Everything above assumes the index already has content in it. Registration
is what puts it there: it's the other half of the feature, hooked into the
request-finished path rather than the request-scheduled path.

**Storage.** Registered chunks live in a *dedicated* ring buffer of
KV-pool slots (`--subcontext-kv-cache-tokens`, default 4096), reserved
once at startup (`Scheduler.maybe_init_subcontext_index`, via
`token_to_kv_pool_allocator.alloc(...)`) and never returned to the
ordinary allocator. This is a deliberate choice over aliasing slots the
radix-cache tree already owns: aliasing would need `SubContextIndex` to be
notified whenever the tree's own eviction policy reclaims an overlapping
node, and there's no such hook today. A dedicated pool sidesteps that
coordination problem at the cost of a fixed, explicit memory reservation,
and reuses the exact same `get_key_buffer`/`get_value_buffer`/
`index_copy_` mechanism `materialize_reused_kv` already uses for
consumption (`kv_materialize.copy_kv_to_new_slots` -- a plain copy, no
repositioning, since the entry is stored at its *original* absolute
position and `reposition_key` corrects it later, at consumption time, for
wherever it's reused).

**What gets registered.** Only explicitly-tagged spans
(`req.subcontext_tags`), currently -- not automatic discovery of arbitrary
recurring content. Automatic *detection* (finding a previously-registered
span again, per the scan step above) doesn't imply automatic
*registration*; deciding what to register without being told needs its
own granularity/dedup/budget policy, which is future work, not something
this pass silently assumes.

**When.** `mem_cache/common.py: register_subcontext_entries(req,
subcontext_index, req_to_token_pool, token_to_kv_pool)` runs from
`managers/scheduler_components/batch_result_processor.py`, at the primary
generation-finished site (`if req.finished(): ...`), *before*
`release_kv_cache(...)` -- that's what frees or reassigns
`req.kv.req_pool_idx`'s row, so registration has to read it first. Only
requests that reach `finished()` (no further retraction possible) are
registered; a still-running request's tagged span is not registered early
and cannot be un-registered later, so registering before it's certain to
survive would risk pointing the index at a span that gets evicted or
retracted out from under it.

**How.** For each tag with `end <= req.effective_kv_committed_len()` (the
same bound `release_kv_cache` itself uses -- a tag reaching further would
read uncommitted, reclaimed, or never-written slots): read the physical
slots for `[start, end)` off `req_to_token_pool.req_to_token[req.kv.req_pool_idx]`,
call `subcontext_index.register(token_ids=..., orig_position=start,
subcontext_id=...)` to reserve ring-buffer space (`None` back means
nothing to do -- already registered, too big for the pool, or no
unreferenced room right now), then `copy_kv_to_new_slots` to actually move
the K/V.

**Tested**: the ring-buffer mechanics in isolation (see above). The
`register_subcontext_entries` orchestration itself needs a real
`req_to_token_pool` row and KV pool to exercise meaningfully, so it's
verified by import/wiring checks here, not a unit test -- the same
GPU-validation gap the rest of this doc already calls out.

### CLI flags (`arg_groups/fields/memory.py`)

- `--enable-subcontext-kv-cache` (default off)
- `--subcontext-brz-window` (default 32, one of `{0, 16, 32, 64}`) -- the
  BRZ window `k`; startup fails if it's any other value
- `--subcontext-kv-cache-tokens` (default 4096) -- size of the dedicated
  registration pool; startup fails if the allocator can't reserve it

Per the env-var-conventions skill, these are CLI flags (user-facing,
expected to be documented and flipped per deployment), not env vars.
Verified end-to-end: `ServerArgs.add_cli_args` / `from_cli_args` parse all
three flags and resolve them onto the flat `ServerArgs` object.

### Scheduler-side matching (`managers/schedule_batch.py`)

`Req.init_next_round_input` gained `subcontext_index` and
`subcontext_brz_window` parameters (both optional, default off, so
every existing call site is unaffected). When a `SubContextIndex` is
passed and the request has unmatched suffix tokens, it resolves
`self.subcontext_tags` into `SubContextTag`s, scans the unmatched suffix,
and stores the result on the request as `req.subcontext_matches` /
`req.subcontext_recompute_plans`.

**Verified**: `schedule_batch.py`, `scheduler.py`, `tokenizer_manager.py`,
and `server_args.py` all import cleanly end-to-end with this change (full
CPU dependency chain resolved and exercised in this repo's sandbox,
without GPU/CUDA) -- the wiring is syntactically and structurally sound
end to end from the HTTP request field down to the per-request match plan.

## How reused tokens are excluded from the live forward pass

This is the mechanism that makes the feature actually save compute rather
than just relabel it -- prefix caching already skips the full per-layer
forward (not just the attention math) for cached tokens, and this
generalizes that to non-leading spans. Two things happen, in order, for a
batch containing subcontext-matched requests:

1. **Materialize reused KV before the model forward.**
   `ScheduleBatch.prepare_for_extend` (`managers/schedule_batch.py`) computes,
   per eligible request, a `RequestSubcontextPlan`
   (`mem_cache/subcontext/extend_plan.py: plan_request_extend`) splitting its
   extend range into `surviving_local` offsets (need a real forward pass)
   and `reused_local` offsets (materialized instead), then
   `build_batch_subcontext_plan` applies that split to the batch's
   already-allocated `out_cache_loc` (allocation itself is untouched --
   sized off the *full* logical range, since every position needs a slot
   either way) and collects a flat `SubcontextMaterializePlan`
   (source slot, dest slot, delta position per reused token) onto
   `batch.subcontext_materialize_plan`. `managers/tp_worker.py:
   forward_batch_generation` calls
   `kv_materialize.materialize_reused_kv_for_batch` with it right after
   `ForwardBatch.init_new(...)` and before `self.model_runner.forward(...)`:
   for every layer, gather the source chunk's K/V via
   `KVCache.get_key_buffer(layer_id)` / `get_value_buffer(layer_id)`,
   reposition K (`layers/rotary_embedding/reposition.py`), and
   `index_copy_` both into the destination slots. Pure data movement, no
   model forward involved, so `model_executor/model_runner.py` (frozen)
   does not change.
2. **Exclude those tokens from the forward pass.** In the same
   `prepare_for_extend` pass, `input_ids` becomes a *gather* over each
   eligible request's `surviving_absolute` positions instead of the plain
   `fill_ids[prefix_len:]` slice, and `extend_lens` /
   `extend_num_tokens` are rebound to the surviving-token counts
   (allocation already happened off the full counts, so this rebind is
   safe). `batch.subcontext_positions` carries the true logical positions
   for those surviving tokens; `ForwardBatch.init_new`
   (`model_executor/forward_batch_info.py`) overrides `ret.positions` with
   it in the same branch that already handles DLLM/spec-info position
   overrides, ahead of the default `compute_position()` arange (which
   still runs, correctly sized off the now-consistent `extend_lens`, but
   is simply discarded). `req_to_token_pool`'s row for each request already
   holds every logical position's slot after allocation regardless of
   which step (materializer or live forward) later fills it, so existing
   attention backends need no change -- they already attend over the full
   row up to `seq_len` (confirmed against `flashattention_backend.py`,
   which derives `cu_seqlens_q` from `extend_seq_lens` and the KV context
   length separately from `seq_lens`, exactly the split this needs).

**Eligibility gate.** `prepare_for_extend` only takes this path for a
request with a non-empty `subcontext_recompute_plans`, no multimodal
inputs, no positional-embed overrides, and only when the whole batch has
`return_logprob=False`, isn't DLLM, and isn't running the mamba extra
buffer -- deliberately conservative so the untouched path (unaffected by
any of this) is guaranteed to run for anything the accounting above
doesn't cover. Below the request level, `reused_ranges_within` also drops
any match that isn't *entirely* inside the current extend range, so a
match split across a chunked-prefill boundary is just computed normally
rather than partially materialized.

**Zero-overhead-when-idle.** Every new code path in `prepare_for_extend` is
gated behind `subcontext_active = any(plan is not None for plan in
subcontext_request_plans)`, itself impossible to be `True` unless a
`SubContextIndex` was constructed (`--enable-subcontext-kv-cache`) *and* a
request actually matched something. Feature off, or on with nothing yet
recurring, is byte-for-byte the pre-existing code path --
`test_build_batch_subcontext_plan_all_passthrough_matches_original`
pins this down for the batch-plan transform itself.

**Startup validation.** `Scheduler.maybe_init_subcontext_index`
(`managers/scheduler.py`) resolves the loaded model's `RotaryEmbedding` via
`kv_materialize.find_rotary_embedding` and raises at startup if the model
doesn't have exactly one shared instance (mixed RoPE bases, MLA, mrope,
NoPE/ALiBi) -- the MVP's dense/standard-RoPE restriction is enforced, not
just documented. It also checks
`kv_materialize.attention_backend_supports_subcontext_reuse` against
`model_runner.prefill_attention_backend_str` (see below) and raises if the
configured backend isn't one of the two verified safe.

## Attention-backend constraint: FlashInfer's ragged prefill path

A review of this feature caught a real gap between "the KV pool has valid
data at every logical position" and "the attention kernel actually reads
it." FlashInfer's default prefill path (`layers/attention/flashinfer_backend.py`)
splits attention into two passes when `use_ragged` is true (the default --
`SGLANG_FLASHINFER_USE_PAGED` defaults to `False`, and `use_ragged` is only
forced off by paged mode, determinism, or a tc_piecewise CUDA graph): a
*ragged* self-attention pass among only this step's own query tokens
(sized and read straight from the fresh K/V, no page table), plus a
*paged* pass over the literal prefix, sized off `extend_prefix_lens` alone
-- never off `seq_lens`. That's exactly right for ordinary prefix caching,
where "this step's tokens" are always the request's full non-cached
suffix. It's wrong here: with subcontext reuse, "this step's tokens" (the
surviving positions) are a non-contiguous subset, and a reused span can
sit in the gap between two surviving positions -- materialized into the KV
pool, but never read by either pass, since the ragged pass never looks at
the pool and the paged pass stops at `extend_prefix_lens`. The reused
chunk would silently drop out of attention entirely.

FlashAttention's extend path (`layers/attention/flashattention_backend.py`)
has no such split: `cu_seqlens_k` / `page_table` are always built off the
full `seq_lens` (via `req_to_token_pool`), with `extend_seq_lens` used only
to size the query side. That is exactly the split this feature needs, and
it's what `attention_backend_supports_subcontext_reuse` allowlists
unconditionally. FlashInfer is allowlisted only when
`SGLANG_FLASHINFER_USE_PAGED=1` forces its ragged path off. Every other
backend (Triton, torch native, ...) is unaudited and therefore refused --
this is a strict allowlist, not a denylist, on purpose.

**Scan boundary.** A second issue from the same review: the scanner had no
analogue of `Req._compute_max_prefix_len`'s `input_len - 1` cap, so a
match could cover a request's very last token, leaving zero surviving
tokens and no logit to sample from. `subcontext_scan` now takes an `end`
parameter and `init_next_round_input` passes `input_len - 1`, exactly
mirroring the ordinary prefix match's rule.

**input_embeds requests** are also excluded from the eligibility gate in
`prepare_for_extend`: their own `input_embeds` slicing assumes the full
extend range, which a gathered/shrunk `input_ids` would silently mismatch.

## File list

New:
- `python/sglang/srt/mem_cache/subcontext/__init__.py`
- `python/sglang/srt/mem_cache/subcontext/subcontext_types.py`
- `python/sglang/srt/mem_cache/subcontext/subcontext_index.py`
- `python/sglang/srt/mem_cache/subcontext/subcontext_scanner.py`
- `python/sglang/srt/mem_cache/subcontext/deviation_recompute.py`
- `python/sglang/srt/mem_cache/subcontext/extend_plan.py` -- per-request and
  per-batch plan builders consumed by `prepare_for_extend`.
- `python/sglang/srt/mem_cache/subcontext/kv_materialize.py` -- the
  materializer, `copy_kv_to_new_slots` (registration's plain copy),
  `find_rotary_embedding`, `attention_backend_supports_subcontext_reuse`,
  and the `tp_worker.py` call-site wrapper.
- `python/sglang/srt/layers/rotary_embedding/reposition.py`
- `test/registered/unit/mem_cache/test_subcontext_cache.py`

Modified:
- `python/sglang/srt/managers/schedule_batch.py` -- `Req.subcontext_tags` /
  `subcontext_matches` / `subcontext_recompute_plans`;
  `init_next_round_input` scan wiring (with the `end` cap); the eligibility
  gate (incl. `input_embeds`), gathered `input_ids`, and
  `out_cache_loc`/`extend_lens` rewrite in `prepare_for_extend`.
- `python/sglang/srt/managers/scheduler.py` -- pass `recv_req.subcontext_tags`
  into `Req(...)`; `Scheduler.maybe_init_subcontext_index` (validates the
  model and attention backend, reserves the registration pool, constructs
  the index); thread `self.subcontext_index` into the main prefill
  admission's `init_next_round_input` call and into
  `SchedulerBatchResultProcessor`.
- `python/sglang/srt/managers/scheduler_components/batch_result_processor.py`
  -- `subcontext_index` field; calls `register_subcontext_entries` at the
  primary generation-finished site, before `release_kv_cache`.
- `python/sglang/srt/mem_cache/common.py` -- `register_subcontext_entries`.
- `python/sglang/srt/managers/tp_worker.py` -- the
  `materialize_reused_kv_for_batch` call site in
  `forward_batch_generation`, right after `ForwardBatch.init_new`.
- `python/sglang/srt/model_executor/forward_batch_info.py` -- the
  `batch.subcontext_positions` override branch in `ForwardBatch.init_new`.
- `python/sglang/srt/managers/io_struct.py` -- `GenerateReqInput.subcontext_tags`,
  `TokenizedGenerateReqInput.subcontext_tags`.
- `python/sglang/srt/managers/tokenizer_manager.py` -- propagate
  `subcontext_tags` into `TokenizedGenerateReqInput`.
- `python/sglang/srt/arg_groups/fields/memory.py` -- the three CLI flags.

## What still needs GPU validation

Every module above is either pure-CPU tested (index, scanner, rotation
math, materializer against a fake pool, the batch-plan transform) or
import/syntax verified end-to-end (this repo's real dependency chain,
resolved in a CPU-only sandbox, imports `schedule_batch.py`, `scheduler.py`,
`tp_worker.py`, `forward_batch_info.py`, `tokenizer_manager.py`, and
`server_args.py` cleanly with these changes). What that cannot cover: an
actual forward pass on a real dense model, confirming the attention
backend's `cu_seqlens_q`/context-length split behaves as analyzed under
`extend_seq_lens[i] != seq_lens[i] - prefix_lens[i]` -- the first time this
codebase produces that inequality for a plain (non-chunked, non-retracted)
extend batch. Recommended smoke test before any real traffic: a small
Llama-style model, a handful of requests that share an explicitly-tagged
sub-context at different offsets, `--subcontext-brz-window 0` (pure
reuse, easiest to reason about), comparing output logits against the same
prompts run without `--enable-subcontext-kv-cache` for a byte-level baseline.
